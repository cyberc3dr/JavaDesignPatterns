# Вопросы по Spring

## Framework

1. Чем фреймворк отличается от библиотеки? Объясните «Голливудский принцип» (Hollywood Principle).
2. Назовите основные преимущества и недостатки использования фреймворков.

## IoC (Inversion of Control)

3. Что такое Inversion of Control? Какую проблему решает IoC?
4. Что такое Spring Container и Spring Bean? Какова связь между ними?
5. Что такое `ApplicationContext`? Какие реализации `ApplicationContext` вы знаете?
6. Назовите три способа конфигурации Spring-контекста. Приведите примеры каждого.
7. Как создать бин через XML-конфигурацию с помощью конструктора с параметрами? Приведите пример.
8. Чем отличается создание бина через внутренний фабричный метод (`factory-method`) от внешнего фабричного метода (`factory-bean` + `factory-method`)?
9. Как работает `@ComponentScan`? Что произойдёт, если не указать `basePackages`?
10. Как совместить XML-конфигурацию и аннотации в одном проекте?

## DI (Dependency Injection)

11. Что такое Dependency Injection и как DI связан с IoC?
12. Назовите три способа внедрения зависимостей (injection). Какой из них рекомендуется Spring и почему?
13. Как настроить DI через XML? Объясните разницу между `constructor-arg ref` и `property ref`.
14. Как работает аннотация `@Autowired`? На каких элементах её можно использовать?
15. Для чего нужны аннотации `@Qualifier` и `@Primary`? Когда использовать каждую из них?
16. Что произойдёт, если Spring найдёт два бина, подходящих для `@Autowired`, и не указан ни `@Qualifier`, ни `@Primary`?

## Profiles

17. Что такое Spring Profiles и какую задачу они решают?
18. Можно ли активировать несколько профилей одновременно?

## Bean Lifecycle

19. Что такое жизненный цикл бина? Опишите его основные этапы.
20. Вызывается ли destroy-метод для бинов со scope `prototype`? Почему?

## Scope

21. Чем Singleton scope отличается от Prototype scope? Сравните по трём критериям: создание, состояние, жизненный цикл.
22. Какой scope у бина по умолчанию? Как изменить scope через XML и через аннотации?
23. Что произойдёт, если Singleton-бин зависит от Prototype-бина? Как решить эту проблему?

## AOP (Aspect Oriented Programming)

24. Что такое AOP? Какие проблемы решают понятия code tangling и code scattering?
25. Перечислите пять типов Advice в Spring AOP. Чем `@Around` отличается от комбинации `@Before` + `@After`?
26. Что такое Pointcut? Объясните синтаксис выражения `execution(* com.example.service.*.*(..))`.
