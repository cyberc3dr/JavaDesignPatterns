# Strategy

## Стратегия

**Паттерн Стратегия (Strategy)** — это поведенческий паттерн проектирования, который определяет семейство алгоритмов,
инкапсулирует каждый из них и делает их взаимозаменяемыми. Стратегия позволяет изменять алгоритмы независимо от
клиентов, которые ими пользуются.

#### Основная идея

Паттерн Стратегия позволяет определить набор взаимозаменяемых алгоритмов, помещая их в отдельные классы, и делать их
взаимозаменяемыми во время выполнения программы. Это достигается путём использования интерфейса или абстрактного класса,
который определяет общий метод для всех алгоритмов.

#### Применение

Паттерн Стратегия рекомендуется использовать в следующих случаях:

- Необходимо выбрать один из нескольких алгоритмов в зависимости от условий во время выполнения.
- Алгоритмы часто изменяются или расширяются.
- Необходимо избежать множественных условных операторов (`if`, `switch`) для выбора алгоритма.
- Хотите обеспечить повторное использование алгоритмов независимо от контекста.

### Реализация

1. Определите алгоритм, который подвержен частым изменениям. Также подойдёт алгоритм, имеющий несколько вариаций,
   которые выбираются во время выполнения программы.
2. Создайте интерфейс стратегий, описывающий этот алгоритм. Он должен быть общим для всех вариантов алгоритма.
3. Поместите вариации алгоритма в собственные классы, которые реализуют этот интерфейс.
4. В классе контекста создайте поле для хранения ссылки на текущий объект-стратегию, а также метод для её изменения.
   Убедитесь в том, что контекст работает с этим объектом только через общий интерфейс стратегий.
5. Клиенты контекста должны подавать в него соответствующий объект-стратегию, когда хотят, чтобы контекст вёл себя
   определённым образом.

### Примеры

#### Примеры из стандартной библиотеки Java

- **`Comparator`** (`java.util`) — классический пример паттерна Стратегия. Позволяет определить различные стратегии
  сравнения объектов при сортировке коллекций.
- **`LayoutManager`** (Swing, `java.awt`) — контейнер (`Container`) делегирует размещение компонентов стратегии
  компоновки (`FlowLayout`, `BorderLayout`, `GridLayout` и др.), которую можно заменить в любой момент.
- **`Collector`** (Stream API, `java.util.stream`) — терминальная операция `collect()` принимает стратегию сборки
  результата (`Collectors.toList()`, `Collectors.groupingBy()` и др.), определяющую, как элементы потока
  объединяются в итоговую структуру.

#### Лямбды как стратегии (Java 8+)

Если интерфейс стратегии содержит ровно один абстрактный метод, он является **функциональным интерфейсом** и может
быть реализован лямбда-выражением или ссылкой на метод. Это избавляет от необходимости создавать отдельный класс
для каждой тривиальной стратегии:

```java
// вместо new AscendingSort()
sorter.setStrategy(items -> Collections.sort(items));

// вместо new LengthSort()
sorter.setStrategy(items -> items.sort(Comparator.comparingInt(String::length)));
```

Лямбды удобны для одноразовых или простых стратегий. Для сложных алгоритмов с собственным состоянием (например,
`PromoCodeDiscount` с суммой скидки в конструкторе) по-прежнему лучше использовать полноценные классы.

---

#### [Пример 1](code%2Fexample1_navigator%2FMain.java) — Навигатор: стратегии маршрутов

Навигатор строит маршрут между двумя точками, делегируя выбор алгоритма стратегии. Пользователь может переключать
вид транспорта (автомобиль, общественный транспорт, пешком) на лету.

- [`RouteStrategy`](code%2Fexample1_navigator%2FRouteStrategy.java) — интерфейс стратегии (Strategy)
- [`RoadStrategy`](code%2Fexample1_navigator%2FRoadStrategy.java) — автомобильный маршрут (ConcreteStrategy)
- [`PublicTransportStrategy`](code%2Fexample1_navigator%2FPublicTransportStrategy.java) — общественный транспорт (ConcreteStrategy)
- [`WalkingStrategy`](code%2Fexample1_navigator%2FWalkingStrategy.java) — пеший маршрут (ConcreteStrategy)
- [`Navigator`](code%2Fexample1_navigator%2FNavigator.java) — навигатор (Context)
- [`Main`](code%2Fexample1_navigator%2FMain.java) — демонстрация переключения стратегий

---

#### [Пример 2](code%2Fexample2_sort%2FMain.java) — Сортировка: стратегии сортировки списка

Создадим простую систему сортировки списка строк с возможностью выбора различных стратегий сортировки. Например, по
возрастанию, по убыванию, по длине. Обратите внимание что сортировка по возрастанию и по убыванию проводится в
лексикографическом порядке.

- [`SortStrategy`](code%2Fexample2_sort%2FSortStrategy.java) — интерфейс стратегии (Strategy)
- [`AscendingSort`](code%2Fexample2_sort%2FAscendingSort.java) — сортировка по возрастанию (ConcreteStrategy)
- [`DescendingSort`](code%2Fexample2_sort%2FDescendingSort.java) — сортировка по убыванию (ConcreteStrategy)
- [`LengthSort`](code%2Fexample2_sort%2FLengthSort.java) — сортировка по длине (ConcreteStrategy)
- [`Sorter`](code%2Fexample2_sort%2FSorter.java) — сортировщик (Context)
- [`Main`](code%2Fexample2_sort%2FMain.java) — демонстрация переключения стратегий

---

#### [Пример 3](code%2Fexample3_discount%2FMain.java) — Скидки: стратегии расчёта скидок

Интернет-магазин применяет разные скидки к заказу в зависимости от статуса покупателя или промоакции.
Стратегия скидки переключается на лету, а контекст (`Order`) не знает деталей расчёта.

- [`DiscountStrategy`](code%2Fexample3_discount%2FDiscountStrategy.java) — интерфейс стратегии (Strategy)
- [`RegularDiscount`](code%2Fexample3_discount%2FRegularDiscount.java) — без скидки / Null-Object (ConcreteStrategy)
- [`StudentDiscount`](code%2Fexample3_discount%2FStudentDiscount.java) — студенческая скидка 15 % (ConcreteStrategy)
- [`PromoCodeDiscount`](code%2Fexample3_discount%2FPromoCodeDiscount.java) — фиксированная скидка по промокоду (ConcreteStrategy)
- [`VipDiscount`](code%2Fexample3_discount%2FVipDiscount.java) — VIP-скидка 25 % (ConcreteStrategy)
- [`Order`](code%2Fexample3_discount%2FOrder.java) — заказ (Context)
- [`Main`](code%2Fexample3_discount%2FMain.java) — демонстрация переключения стратегий

### Плюсы данного паттерна

- **Гибкость:** Позволяет легко изменять поведение объекта путём замены используемой стратегии.
- **Расширяемость:** Легко добавлять новые стратегии без изменения существующего кода (Open/Closed Principle).
- **Снижение связанности:** Контекст и стратегии слабо связаны, что облегчает поддержку и расширение системы.
- **Повторное использование:** Одна и та же стратегия может использоваться в разных контекстах.

### Недостатки данного паттерна

- **Увеличение числа классов:** Каждая новая стратегия требует создания отдельного класса, что может привести к
  увеличению количества классов в проекте.
- **Сложность управления стратегиями:** Необходимо обеспечить правильное управление и выбор стратегий, особенно если их
  много.
- **Избыточность при малом числе алгоритмов:** Если вариантов всего два и они не меняются, паттерн может быть
  избыточным — достаточно простого `if`/`else`. Стратегия окупается, когда алгоритмов три и более или предполагается
  расширение.
- **Потенциальные проблемы с производительностью:** Создание большого числа объектов-стратегий может привести к
  увеличению потребления памяти.

### Сравнение с другими паттернами

Паттерны **Состояние** и **Стратегия** похожи, а их UML-диаграммы по сути одинаковы.
Однако между ними есть принципиальные различия:

| Критерий | Strategy | State |
|---|---|---|
| **Цель** | Выбрать один алгоритм из семейства | Изменить поведение при смене состояния |
| **Кто инициирует смену** | Клиент (внешний код) передаёт нужную стратегию | Сам объект (или состояние) переключает состояние |
| **Осведомлённость** | Стратегии не знают друг о друге | Состояния часто знают о соседних состояниях и инициируют переходы |
| **Количество активных** | Ровно одна стратегия в каждый момент | Ровно одно состояние, но переходы — часть логики |
| **Типичный сценарий** | Сортировка, маршрутизация, расчёт скидок | Заказ (новый → оплачен → доставлен), TCP-соединение |

Паттерн **Стратегия** инкапсулирует алгоритмы. Из множества стратегий выберите ту, которая решает вашу задачу наиболее
эффективно.

Паттерн **Состояние** используется для инкапсуляции характеристик состояния. Поведение объекта
зависит от его состояния. Когда объект находится в определённом состоянии, он ведёт себя иначе, чем
когда он находится в другом состоянии.

### Заключение

**Паттерн Стратегия (Strategy)** является мощным инструментом для управления поведением объектов и выбора алгоритмов во
время выполнения программы. Он обеспечивает гибкость, расширяемость и снижает связанность между компонентами системы.

Однако, при использовании паттерна Стратегия важно учитывать его преимущества и недостатки, чтобы не создавать
избыточную структуру классов в простых системах.

### Источники

- Design Patterns with Java: Strategy
- Введение в паттерны проектирования: Стратегия
